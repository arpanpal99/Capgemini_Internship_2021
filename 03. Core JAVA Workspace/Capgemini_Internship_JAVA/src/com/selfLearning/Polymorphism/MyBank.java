package com.selfLearning.Polymorphism;

abstract public class MyBank {
	int getInterest() {
		return 0;
	}

	/*
	 * abstract int getInterest();
	 * 
	 * We can declare an OBJECT or a METHOD of a class as abstract only if the Class
	 * itself is abstract And for an abstract method there's no need to instantiate
	 * it Thereby, the abstract method in LINE 9 can be a valid replacement for the
	 * non-abstract method in LINE 4
	 * 
	 */
}
